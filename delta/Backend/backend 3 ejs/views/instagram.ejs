<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>instagram</title>
    <link rel="stylesheet" href="/style.css">

</head>
<body>
    <h1>hello insta @<%=  data.name %></h1>
    <button>follow</button>

    <!-- btw < %-  %> is used for returning the html code same as < %=  %> this is used for returning string value -->
    <!-- include('kjnf') is used when we want to get the html code from a file...address is passed -->

    <h2>followers <%= data.followers %> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <%= data.following %></h2>
    <hr>

    <h2>posts</h2>
    <% for (let post of data.posts) { %>
        <img src="<%=post.image%>" alt="here was the post">
        <p>Likes: <%=post.likes%></p>
        <p>Comments: <%=post.comments%></p>
        
    <% } %>
</body>
<script src="/app.js"></script>
</html>